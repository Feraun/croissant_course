<h1>For start</h1>

Go to app folder

```docker build -t spring-app .```

After that

```docker-compose up```

Spring work at ```localhost:8080```

PostgreSQL: ```localhost:5432```

UI for Minio: ```localhost:9001```

You can test app with <b>Postman</b>
